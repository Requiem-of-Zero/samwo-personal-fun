global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "restaurant-node-exporter"
    static_configs:
      - targets: ["node-exporter:9100"]
        labels:
          restaurant: "${restaurant_key}"
          domain: "${restaurant.domain}"

remote_write:
  - url: "${remote_write_url}"
